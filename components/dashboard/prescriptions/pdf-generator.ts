/**
 * PDF generation entry (Phase 17 folder contract).
 * Implementation is server-only under features/prescriptions.
 */
export { generatePrescriptionPdf } from "@/features/prescriptions/services/generate-pdf";
