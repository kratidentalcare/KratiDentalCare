/**
 * Scheduling feature — dynamic availability engine + admin configuration.
 *
 * UI components live under `./components`.
 * Server Actions live under `./actions`.
 * Business logic lives under `./services` and `./engine`.
 */

export type {
  AvailabilityResult,
  ClinicAvailabilityFormValues,
  GeneratedSlot,
  HolidayListItem,
  OverrideListItem,
} from "./types";
