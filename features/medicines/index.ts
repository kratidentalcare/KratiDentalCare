/**
 * Medicine library feature — admin catalog + prescription search.
 *
 * UI components live under `./components`.
 * Server Actions live under `./actions`.
 * Business logic lives under `./services`.
 */

export type {
  MedicineListItem,
  MedicineListResult,
  MedicineSearchHit,
} from "./types";
